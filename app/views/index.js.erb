$('.review-container').append('<%= j render @revies %>');
<% if @reviews.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate @reviews %>');
<% else %>
  $(window).off('scroll');
  $('.pagination').remove();
<% end %>
